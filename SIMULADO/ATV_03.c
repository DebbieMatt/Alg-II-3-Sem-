// Bartolomeu é um estudante de programação que esta aprendendo a lidar com aritmética de ponteiros. Atualmente, o seu código está no seguinte estagio de implementação:

#include <stdio.h>
int main(void)
{
    int vetor[3] = {7, 900, 125};
    printf("0 elemento da terceira posição é: %d\n", *(vetor + 2));
}

// Qual fragmento de código deve ser inserido no espaço para que o elemento da terceira posição do vetor seja impresso corretamente?

// Resposta: *(vetor + 2).